import { redirect } from 'next/navigation'; import { isAdmin } from '@/lib/admin-auth'; import SystemWorkspace from '@/components/SystemWorkspace'; export const dynamic='force-dynamic'; export default async function Settings(){if(!await isAdmin())redirect('/admin/login');return <SystemWorkspace/>}
