import { redirect } from 'next/navigation'; import { isAdmin } from '@/lib/admin-auth'; import { SeoWorkspace } from '@/components/GrowthWorkspace'; export const dynamic='force-dynamic'; export default async function Seo(){if(!await isAdmin())redirect('/admin/login');return <SeoWorkspace/>}
