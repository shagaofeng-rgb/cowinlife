'use client';
import { useState } from 'react';
export default function Login() { const [email,setEmail] = useState('info@cowinlife.com'); const [password,setPassword] = useState(''); const [error,setError] = useState(''); const submit = async (event: React.FormEvent) => { event.preventDefault(); const response = await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})}); if (response.ok) location.assign('/admin'); else setError('账号或密码错误'); }; return <main className="admin-login"><form onSubmit={submit}><p>COWINLIFE</p><h1>企业后台</h1><label>邮箱<input required type="email" value={email} onChange={event => setEmail(event.target.value)} /></label><label>密码<input required type="password" value={password} onChange={event => setPassword(event.target.value)} /></label>{error ? <small>{error}</small> : null}<button>登录</button></form></main>; }
