import { redirect } from 'next/navigation'; import { isAdmin } from '@/lib/admin-auth'; import { dashboardData } from '@/lib/analytics'; import { AnalyticsWorkspace } from '@/components/GrowthWorkspace'; export const dynamic='force-dynamic'; export default async function Analytics(){if(!await isAdmin())redirect('/admin/login');return <AnalyticsWorkspace data={await dashboardData('30d')}/>}
